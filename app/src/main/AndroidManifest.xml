<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="com.handmadeapp.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HandMadeApp">

        <!-- Import Activity (интеграционный экран) -->
        <activity
            android:name="com.appforcross.editor.ui.import.ImportActivity"
            android:exported="false" />

        <activity
            android:name="com.appforcross.editor.dev.DevMenuActivity"
            android:exported="false" />

        <!-- Если у вас есть лаунчер-активити, раскомментируйте и укажите её класс:
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        -->
    </application>

</manifest>
